import 'package:flutter/material.dart';

class KetangpaiSuggestionsPage extends StatelessWidget {
  const KetangpaiSuggestionsPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('试卷内容显示'),
    );
  }
}
