import 'package:flutter/material.dart';

class KetangpaiSourcePage extends StatelessWidget {
  const KetangpaiSourcePage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('资料'),
      ),
    );
  }
}
