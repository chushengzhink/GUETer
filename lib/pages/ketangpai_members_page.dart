import 'package:flutter/material.dart';

class KetangpaiMembersPage extends StatelessWidget {
  const KetangpaiMembersPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('成员列表'),
      ),
    );
  }
}
