import 'package:flutter/material.dart';

class KetangpaiAnnouncementPage extends StatelessWidget {
  const KetangpaiAnnouncementPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('公告'),
      ),
    );
  }
}
