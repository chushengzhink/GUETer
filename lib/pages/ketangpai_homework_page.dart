import 'package:flutter/material.dart';

class KetangpaiHomeworkPage extends StatelessWidget {
  const KetangpaiHomeworkPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('作业'),
      ),
    );
  }
}
