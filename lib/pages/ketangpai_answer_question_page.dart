import 'package:flutter/material.dart';

class KetangpaiAnswerQuestionPage extends StatelessWidget {
  const KetangpaiAnswerQuestionPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('互动答题'),
      ),
    );
  }
}
